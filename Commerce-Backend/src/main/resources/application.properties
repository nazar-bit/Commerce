spring.application.name=WindFarm-Backend

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:ecommerce_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:4523}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

app.db.seed.enabled=${SEED_DB:true}